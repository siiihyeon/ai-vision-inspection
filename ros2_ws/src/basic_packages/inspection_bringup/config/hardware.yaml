/**:
  ros__parameters:
    profile: hardware

/inspection/master_node:
  ros__parameters:
    comm.master_heartbeat_period_ms: 500
    system.expected_interface_version: "1.0.0"

/inspection/control_node:
  ros__parameters:
    comm.node_heartbeat_period_ms: 500

/inspection/vision_node:
  ros__parameters:
    comm.node_heartbeat_period_ms: 500

/inspection/log_node:
  ros__parameters:
    comm.node_heartbeat_period_ms: 500

# TODO(HARDWARE_REQUIRED): Mega serial port와 safe output 핀
# TODO(HARDWARE_REQUIRED): TB6600 steps/mm, 속도, 촬영 위치 offset
# TODO(HARDWARE_REQUIRED): 센서 디바운스·CLEAR·재무장·stuck 기준
# TODO(HARDWARE_REQUIRED): 카메라 ID·역할·트리거·조명·임계값
# TODO(HARDWARE_REQUIRED): 액추에이터 거리·전환·통과 확인 기준
# 각 작업 노드가 required_hardware_parameters()를 구현하기 전에는 초기화가 INIT_BLOCKED 됩니다.
